spring:
  application:
     name: configuration-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: true

encrypt:
  key-store:
    location: classpath:default-key.jks
    password: password
    alias: default
    secret: password