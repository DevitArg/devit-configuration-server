server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DevitArg/uservices-config.git
          searchPaths: '{application}'
        encrypt:
          enabled: true

